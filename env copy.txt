# ---- Database ----
# Postgres connection string. Use a managed DB in production.
DATABASE_URL="postgresql://dima@localhost:5432/chores?schema=public"

# ---- Session signing ----
# Run:  openssl rand -base64 48   to generate a strong value.
# Must be at least 32 characters. Changing this will log everyone out.
AUTH_SECRET="HeDithlzYRYowkvinyfHzyA5xuPLYx3T8vHNcclUNYHRUXRmDGe2u27NO211GOEj"

# ---- Bootstrap token ----
# Used only on /setup to create the very first parent account.
# Generate:  openssl rand -hex 24
# Rotate (or unset) after the first parent is created.
BOOTSTRAP_TOKEN="f2899d2966f2f717718b90bbf376f45ce50b4a3913178777"

# ---- App settings ----
APP_TIMEZONE="Europe/Moscow"
PARENT_SESSION_DAYS="7"
CHILD_SESSION_DAYS="30"

# ---- Deployment ----
# The base URL of the deployed app (used for absolute URLs where needed).
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Set to "true" in production; affects cookie Secure flag.
# NODE_ENV is already set by Next/Vercel — this is a hint only.



# ---- Database ----
# Postgres connection string. Use a managed DB in production.
DATABASE_URL="postgresql://neondb_owner:npg_EHdPv10NTkhU@ep-square-salad-alaepkug-pooler.c-3.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require&pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://neondb_owner:npg_EHdPv10NTkhU@ep-square-salad-alaepkug.c-3.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# ---- Session signing ----
# Run:  openssl rand -base64 48   to generate a strong value.
# Must be at least 32 characters. Changing this will log everyone out.
AUTH_SECRET="HeDithlzYRYowkvinyfHzyA5xuPLYx3T8vHNcclUNYHRUXRmDGe2u27NO211GOEj"

# ---- Bootstrap token ----
# Used only on /setup to create the very first parent account.
# Generate:  openssl rand -hex 24
# Rotate (or unset) after the first parent is created.
BOOTSTRAP_TOKEN="f2899d2966f2f717718b90bbf376f45ce50b4a3913178777"

# ---- App settings ----
APP_TIMEZONE="Europe/Moscow"
PARENT_SESSION_DAYS="7"
CHILD_SESSION_DAYS="30"

# ---- Deployment ----
# The base URL of the deployed app (used for absolute URLs where needed).
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Set to "true" in production; affects cookie Secure flag.
# NODE_ENV is already set by Next/Vercel — this is a hint only.